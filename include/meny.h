#ifndef START_MENY_H
#define START_MENY_H 

#include <SDL.h>
#include <SDL_ttf.h>
#include <stdbool.h>

// Forward declaration of the struct
/*typedef struct startButton StartButton;
// Function prototypes
StartButton *createStartButton(SDL_Renderer *renderer, TTF_Font *font, const char *text, int x, int y, int w, int h, SDL_Color btnColor, SDL_Color textColor);
void renderStartButton(StartButton *button); //ritar knappen
bool isMouseOverStartButton(StartButton *button, int mouseX, int mouseY); //tittar om musen är på knappen eller inte
void destroyStartButton(StartButton *button);*/

#endif

